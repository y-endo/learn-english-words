import * as React from 'react';

import DefaultLayout from '~/layouts/default';

const PlayPage: React.FC = () => {
  const content = <div>play</div>;

  return <DefaultLayout content={content} />;
};

export default PlayPage;
